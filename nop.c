#include "monty.h"
/**
  *f_nop- imagine it does nothing
  *@head: this is the head for nithing
  *@counter: this is the line number
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
