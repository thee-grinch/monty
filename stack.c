#include "monty.h"
/**
 * f_stack - Functiion that does nothind
 * @head: the head of the stack
 * @counter: this is the lline number
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
